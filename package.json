{
  "name": "example-react-lib",
  "version": "1.0.0",
  "description": "",
  "targets": {
    "src": {
      "source": "src/index.ts",
      "isLibrary": true,
      "distDir": "dist"
    },
    "example": {
      "source": "example/index.html",
      "isLibrary": false,
      "distDir": "dist"
    }
  },
  "exports": {
    ".": "./dist/*",
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "react-scripts test",
    "build": "npx parcel build --target src",
    "start": "npx parcel --target example --no-cache",
    "build-example": "npx parcel build --target example --no-cache --no-source-maps"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@parcel/packager-ts": "2.8.3",
    "@parcel/transformer-typescript-types": "2.8.3",
    "parcel": "^2.8.3",
    "process": "^0.11.10",
    "typescript": "^4.9.5"
  }
}